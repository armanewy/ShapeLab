# Foundry Product Quality Integration Report

## Summary

This integration branch is not ready for `main`.

The non-UI product-quality branches were merged and the required automated gates
for those branches pass on the integration branch when run with an
integration-local Cargo target directory.

The Unified Make Canvas UI branch was not merged because its mandatory
screenshot gate was interrupted before completion and Computer Use cannot be
initialized in this thread. The product requirement that Directions and
Customize become one Make screen is therefore still not satisfied on this
integration branch.

## Branches

Merged:

- `codex/candidate-legibility-engine-v1`
- `codex/scifi-crate-hq-authoring-agents`
- `codex/roman-bridge-hq-authoring-agents`
- `codex/stylized-lamp-hq-authoring-agents`
- `codex/catalog-visual-quality-curation`
- `codex/surface-candidate-backend-v0`
- `codex/rig-motion-readiness-cleanup`

Not merged:

- `codex/unified-make-canvas-ui`

Reason:

- The branch is uncommitted.
- Its non-visual `shape-app` checks passed after one local clippy fix.
- Its screenshot artifacts were invalid 15 x 15 PNGs.
- The mandatory screenshot gate could not be completed.

## Automated Gate Results

Commands were run from:

```text
C:\Users\aoztu\Documents\ShapeLab-worktrees\foundry-product-quality-integration
```

Final integration verification used:

```text
CARGO_TARGET_DIR=C:\Users\aoztu\Documents\ShapeLab-worktrees\foundry-product-quality-integration\target
```

This avoided stale artifacts from earlier parallel branch verification.

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | Pass |
| `cargo test -p shape-app foundry --jobs 1` | Pass |
| `cargo test -p shape-search foundry --jobs 1` | Pass |
| `cargo test -p shape-render foundry --jobs 1` | Pass |
| `cargo test -p shape-foundry-catalog --test scifi_crate --jobs 1` | Pass |
| `cargo test -p shape-foundry-catalog --test roman_bridge --jobs 1` | Pass |
| `cargo test -p shape-foundry-catalog --test stylized_lamp --jobs 1` | Pass |
| `cargo test -p shape-gamekit surface --jobs 1` | Pass |
| `cargo test -p shape-gamekit rig --jobs 1` | Pass |
| `cargo test -p shape-gamekit motion --jobs 1` | Pass |
| `cargo test -p shape-character --jobs 1` | Pass |
| `cargo test -p shape-cli game_ready_static --jobs 1` | Pass |
| `cargo clippy --workspace --all-targets -- -D warnings` | Pass |
| `cargo build --release --workspace` | Pass |

Additional full catalog run:

```text
cargo test -p shape-foundry-catalog --jobs 1
```

Result:

- Failed in the existing `moba_hero` candidate-count tests.
- `moba_hero_candidate_modes_return_six_survivors_with_human_explanations`
  returned 0 silhouette candidates instead of 6.
- `moba_hero_locks_are_respected_by_candidate_generation` returned 4
  candidates instead of 6.

These failures are consistent with the stricter legibility engine and the
catalog curation decision to keep weak profiles out of the default novice
catalog. They remain visible and should be addressed separately.

## Screenshot And Video Gate

Required screenshots and video were not captured.

Computer Use bootstrap failed twice with:

```text
failed to write kernel assets: The system cannot find the path specified. (os error 3)
```

The Unified Make Canvas worker also reported that its Computer Use session was
stopped by the physical Escape key before the screenshot sequence completed.

Invalid partial screenshot artifacts existed in the UI worktree:

```text
C:\Users\aoztu\Documents\ShapeLab-worktrees\unified-make-canvas-ui\target\make-canvas-screenshots\01_choose.png
C:\Users\aoztu\Documents\ShapeLab-worktrees\unified-make-canvas-ui\target\make-canvas-screenshots\02_make_initial_crate.png
C:\Users\aoztu\Documents\ShapeLab-worktrees\unified-make-canvas-ui\target\make-canvas-screenshots\03_make_generated_whole_asset_ideas.png
```

Those files were only 15 x 15 pixels and are not valid product evidence.

Sci-Fi Crate headless visual evidence was generated by the authoring worker:

```text
C:\Users\aoztu\Documents\ShapeLab-worktrees\scifi-crate-hq-authoring-agents\target\foundry-benchmark\scifi-crate-hq\parent.png
C:\Users\aoztu\Documents\ShapeLab-worktrees\scifi-crate-hq-authoring-agents\target\foundry-benchmark\scifi-crate-hq\control-endpoint-strip.png
C:\Users\aoztu\Documents\ShapeLab-worktrees\scifi-crate-hq-authoring-agents\target\foundry-benchmark\scifi-crate-hq\explore-contact-sheet.png
C:\Users\aoztu\Documents\ShapeLab-worktrees\scifi-crate-hq-authoring-agents\target\foundry-benchmark\scifi-crate-hq\option-gallery-contact-sheet.png
C:\Users\aoztu\Documents\ShapeLab-worktrees\scifi-crate-hq-authoring-agents\target\foundry-benchmark\scifi-crate-hq\legibility-report.json
```

This is useful asset evidence, but it does not replace the required product UI
screenshot/video gate.

## Product Truth Check

| Requirement | Status |
| --- | --- |
| Directions and Customize are no longer separate primary screens | Fail |
| Make is the primary working screen | Fail |
| Variation Mode jargon is not product-visible | Blocked with UI branch |
| Surface candidate UI is disabled unless visual candidates are integrated | Pass |
| Rig/motion readiness remains hidden from novice UI | Pass |
| Full game-ready remains blocked without manual review and engine proof | Pass |

## Adversarial Review

Does this still look like a settings form?

- Without the Make Canvas UI branch, yes. The app still has the old primary
  screen split.

Does the user understand what changed?

- The stricter legibility engine and HQ catalog branches improve the underlying
  candidate quality, but this cannot be accepted without the product UI
  screenshot gate.

Are candidate differences real or text-only?

- The Sci-Fi Crate, Roman Bridge, and Stylized Lamp family tests now assert
  visible endpoint and candidate differences. Product screenshots are still
  missing.

Are the meshes good enough to make the UI meaningful?

- The three HQ families are improved and pass their focused tests. `moba_hero`
  remains weak under strict legibility and should stay hidden from the novice
  catalog.

Is the app still exposing future features before they are useful?

- Surface and rig/motion readiness remain hidden or truthfully blocked in the
  product-facing flow.

Would a noob keep using this after 2 minutes?

- Unknown. The current integration branch lacks the Make Canvas UI and cannot
  pass the required dogfood screenshot/video review.

## Remaining Blockers

1. Finish and commit `codex/unified-make-canvas-ui`.
2. Capture valid Make Canvas screenshots and a short video.
3. Merge the UI branch into `codex/foundry-product-quality-integration`.
4. Re-run the Prompt 9 automated gate list after UI merge.
5. Re-run the screenshot/video gate with Computer Use or a working manual
   capture path.
6. Keep `moba_hero` hidden or update its tests/profile to match the stricter
   candidate legibility policy.

## Merge Recommendation

Do not merge this branch to `main`.

The non-UI implementation stack is healthy, but the product-quality goal is not
met until the Make Canvas UI is merged and verified with real screenshots/video.
